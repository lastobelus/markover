Refresh canonical Markover to current `origin/main` without starting an issue. Start with `npm --silent run markover -- canonical doctor`; its configured checkout owns the operation regardless of the current worktree. Preserve all reviews, wait for active CLI requests, fast-forward that checkout, then run `npm --silent run markover -- canonical refresh` from the current checkout. Complete only when `canonical doctor` reports healthy, its exact expected handler owns `markover:`, and an exact `markover://review/<known-review-id>` URI selects that review. Also verify the renderer uses `markover-app://app` and the diagnostic pill opens Finder. Treat a visible window or manual review selection as diagnostic evidence only.
