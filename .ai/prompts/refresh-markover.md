Refresh canonical Markover to current `origin/main` without starting an issue. Start with `npm --silent run markover -- canonical doctor`; its configured checkout owns the operation regardless of the current worktree. Preserve all reviews, wait for active CLI requests, fast-forward that checkout, then run `npm --silent run markover -- canonical refresh` from the current checkout. Complete only when the refresh and follow-up doctor report a healthy checkout, build, service, and routing with window status `electron-visible`, and an exact `markover://review/<known-review-id>` URI selects that review. Also verify the renderer uses `markover-app://app` and the diagnostic pill opens Finder. `electron-visible` means Electron ordered the window visible; when startup or activation behavior changed, complete the native recovery QA in `docs/developer/development.md` before accepting onscreen presentation.
